export enum Role {
  ADMIN = 'admin',
  USER = 'user',
}
export enum Resource {
  USER = 'user',
}
export enum Permission {
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
}
